CREATE TABLE IF NOT EXISTS `ut_pop` (
    `id` int(11) NOT NULL,
    `name` varchar(32) NOT NULL,
    `url` varchar(128) NOT NULL,
    `date` int(10) NOT NULL,
    PRIMARY KEY (`id`)
) ENGINE=MyISAM DEFAULT CHARSET=utf8;

